# Leave blank for a same-origin deployment where Express serves the frontend.
# For separate frontend/backend hosts, use the full API prefix, e.g.:
# VITE_API_URL=https://api.your-domain.com/api
VITE_API_URL=